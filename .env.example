PORT=8080
NODE_ENV=development

# Firebase Admin (server) — service account JSON path or inline JSON
FIREBASE_SERVICE_ACCOUNT_PATH=./service-account.json

# Firebase Web (client) — exposed to the browser via webpack DefinePlugin
FIREBASE_API_KEY=
FIREBASE_AUTH_DOMAIN=
FIREBASE_PROJECT_ID=
FIREBASE_APP_ID=
